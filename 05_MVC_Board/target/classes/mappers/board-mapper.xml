<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
	
	<insert id="insert" parameterType="Board" useGeneratedKeys="true" keyProperty="no" keyColumn="no"> <!-- useGeneratedKeys="true" keyProperty="no" keyColumn="no" insert때 바로 가져와주면 좋음 -->
		INSERT INTO board(no, title, content, writer)
		VALUES(SEQ_BOARD.NEXTVAL, #{title}, #{content}, #{writer})
	</insert>
	
	  
	<select id="selectAll" resultType="Board">
		SELECT no, title, content, writer, regdate
		FROM board
	</select>
	
	<!--
	
	<select id="selectBoard" parameterType="Board" resultMap="BoardMap">
		SELECT 
	</select>
	
	<update id="update">
		UPDATE board
	</update>
	
	<delete id="delete"></delete>
	
	-->
</mapper>